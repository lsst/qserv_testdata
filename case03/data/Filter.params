[table_info]

tableName: Filter 

schemaFile: Filter.sql

# by default, every table inherits partitioningStrategy
# from its database. If table is not partitioned, we
# turn it off by specifying "off" here.
# Allowed values are: "on", "off"
partitioning: off

