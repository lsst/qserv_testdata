SELECT count(*)
FROM   {DBTAG}.Object o
       INNER JOIN {DBTAG}.RefObjMatch o2t ON (o.objectId = o2t.objectId)
       LEFT  JOIN {DBTAG}.SimRefObject t ON (o2t.refObjectId = t.refObjectId)
WHERE  closestToObj = 1
    OR closestToObj is NULL
